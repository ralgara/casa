# Bootstrapping MacOS

## Install virtualenv
sudo -H python3 -m pip install virtualenv

config 
  k8s context
  ssh
  rc files
  
AWS
 aws vpn
 aws cli
 aws-mfa
 kubectl

shell
 j
 bash
 tmux

apps
 vscode
 lens (
 postmen (
